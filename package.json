{
    "name": "event-ai",
    "version": "1.0.0",
    "private": true,
    "description": "Event-AI - Full Stack Application",
    "scripts": {
        "build": "turbo run build",
        "dev": "turbo run dev",
        "lint": "turbo run lint",
        "format": "prettier --write \"**/*.{ts,tsx,md}\""
    },
    "devDependencies": {
        "concurrently": "^9.1.2",
        "turbo": "latest",
        "prettier": "^3.0.0"
    },
    "engines": {
        "node": ">=20.0.0"
    },
    "overrides": {
        "nodemailer": "7.0.13"
    }
}